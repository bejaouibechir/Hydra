# SPRINT 1 - BACKLOG FINAL

## Mode Upsert MySQL/MariaDB Production-Ready

**Projet** : Hydra ETL  
**Période** : 02–04 Janvier 2026  
**Durée** : 8–10 heures  
**Statut** : ✅ Validé Consortium

---

## 🎯 OBJECTIFS

**Principal** : Mode upsert MySQL/MariaDB fonctionnel  
**Secondaire** : Fondations extensibilité (registry factory)

---

## 📋 PRINCIPES DIRECTEURS

- ✅ Extension additive du DSL (mode upsert + key) - 100% rétrocompatible
- ❌ Aucun nouveau connecteur (MySQL/MariaDB + CSV uniquement)
- ✅ Priorité absolue : qualité, tests, documentation
- ❌ Pas d'implémentation adapter concret (interface vide seulement si temps)
- ⏱️ Estimations incluent écriture + tests + debug

---

## ## 📊 BACKLOG DÉTAILLÉ

| #       | P   | Tâche                               | Description                                                                                                       | Fichier                                         | Temps   | Livrables                                                    | DoD                                         |
| ------- | --- | ----------------------------------- | ----------------------------------------------------------------------------------------------------------------- | ----------------------------------------------- | ------- | ------------------------------------------------------------ | ------------------------------------------- |
| **1.1** | P0  | Registry Factory Pattern            | Remplacer dispatch hardcodé par dictionnaire de factories pour ajouter nouveaux connecteurs sans modifier le core | `internal/connector/registry.py`                | 40-50m  | `CONNECTOR_REGISTRY` dict + factories (csv, mysql)           | +1 ligne = nouveau type sans modifier core  |
| **1.2** | P0  | LoadConfig extension (upsert + key) | Ajouter mode upsert au DSL avec validation stricte du champ key obligatoire                                       | `internal/parser/destination.py`                | 45-60m  | `LoadMode` enum + `key: List[str]` + validation              | Pydantic échoue si upsert sans key          |
| **2.1** | P0  | Génération SQL upsert MySQL         | Générer syntaxe INSERT...ON DUPLICATE KEY UPDATE pour clés simples et composites                                  | `internal/connector/mysql_mariadb_connector.py` | 90-120m | `_build_upsert_sql()` → `INSERT ... ON DUPLICATE KEY UPDATE` | SQL correct clés simples/composites + tests |
| **2.2** | P0  | load_batches() mode upsert          | Intégrer logique upsert dans le chargement par batch avec gestion transactionnelle                                | `internal/connector/mysql_mariadb_connector.py` | 60-90m  | Branchement mode + transactions                              | Insertion idempotente + rollback correct    |
| **2.3** | P0  | Validation executor capabilities    | Vérifier au runtime que destination supporte le mode demandé et que colonnes key existent                         | `internal/runner/executor.py`                   | 30-45m  | `_validate_destination_mode()` + validation colonnes key     | Erreur claire si upsert CSV ou key absente  |
| **3.1** | P0  | Tests unitaires SQL                 | Valider génération SQL pour tous cas (clés simple/composite, erreurs)                                             | `tests/unit/test_mysql_upsert_sql.py`           | 45-60m  | 6-8 tests (clés simple/composite/erreurs)                    | Couverture >90% génération SQL              |
| **3.2** | P0  | Tests E2E CSV→MySQL upsert          | Prouver idempotence upsert avec fixtures Docker sur cas réel CSV→MySQL                                            | `tests/integration/test_upsert_csv_to_mysql.py` | 90-120m | Fixtures Docker + 2 runs idempotents                         | Assert lignes identiques après 2 runs       |
| **3.3** | P0  | Tests parser destination            | Valider parsing modes avec tous cas limites (upsert sans key, modes existants)                                    | `tests/unit/test_destination_parser.py`         | 20-30m  | 4 tests validation modes                                     | Validation Pydantic exhaustive              |
| **4.1** | P1  | Exemple CSV→MySQL upsert            | Créer exemple complet fonctionnel pour démontrer usage upsert aux utilisateurs                                    | `examples/mysql_demos/csv_to_mysql_upsert/`     | 30-45m  | Dossier complet exécutable                                   | Exemple fonctionne via CLI                  |
| **4.2** | P1  | README section Load Modes           | Documenter les 3 modes (append/replace/upsert) avec syntaxe et cas d'usage                                        | `README.md` + `docs/load-modes.md`              | 30-45m  | Tableau modes + syntaxe + exemples                           | Documentation claire                        |
| **5.1** | P2  | (Optionnel) Interface TableAdapter  | Préparer interface vide pour adapters futurs non-tabulaires sans implémentation                                   | `internal/adapters/__init__.py`                 | 20-30m  | Interface vide + doc pattern                                 | Si temps restant uniquement                 |

## ⏱️ PLANNING

- **Minimum** : 7h (P0 strict)
- **Réaliste** : 8-10h (avec debug Docker)
- **Format** : 1 jour complet ou 2 demi-journées

**Risques** :

- Docker MySQL (seed, volumes)
- Bugs clés composites
- Tests intégration lents

**Plan de coupe si retard** :

1. 5.1 Interface Adapter → Skip
2. 4.2 README → Version minimale

---

## ✅ DEFINITION OF DONE (Sprint)

1. ✅ Mode upsert MySQL/MariaDB idempotent validé
   - Table cible possède PRIMARY KEY ou UNIQUE INDEX
2. ✅ Tests unitaires SQL + E2E verts (Docker)
3. ✅ Exemple CSV→MySQL upsert fonctionnel
4. ✅ Registry factory extensible (nouveau type = +1 ligne)
5. ✅ Aucun breaking change DSL
6. ✅ README + exemples à jour

---

## 🔧 DÉTAILS TECHNIQUES CRITIQUES

### **Tâche 2.3 - Validation Executor (Précision)**

**Validations requises** :

```python
def _validate_destination_mode(dest_connector, load_config):
    # 1. Mode supporté par connecteur
    if mode == "upsert" and not dest_connector.capabilities.supports_upsert:
        raise ValueError(f"Destination does not support upsert")

    # 2. Colonnes key existent dans DataFrame
    missing_keys = set(load_config.key) - set(df.columns)
    if missing_keys:
        raise ValueError(f"Key columns {missing_keys} not found in data")
```

### **DoD 1.1 - Pré-requis MySQL Upsert**

**Validation table cible** :

- Table doit avoir PRIMARY KEY ou UNIQUE INDEX
- Colonnes `key` doivent correspondre à cet index
- Tests doivent vérifier cette contrainte

---

## 📝 FEEDBACKS INTÉGRÉS

**ChatGPT** :

- ✅ Clarification "extension additive"
- ✅ Pré-requis MySQL PRIMARY KEY
- ✅ Validation colonnes key absentes
- ✅ Plan de coupe si retard

**Consortium** :

- ✅ Focus strict upsert
- ✅ Préparation minimale extensibilité
- ✅ Pas de scope creep

---

## 🚀 PRÊT POUR EXÉCUTION

**Prochaine étape** : Lancer implémentation  
**Document de référence** : Vision Stratégique (guidance indirecte)

---

**Version** : 1.0 Final  
**Date** : 02 Janvier 2026  
**Validé par** : Consortium Hydra ETL