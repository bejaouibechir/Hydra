# Sources pour démo : MySQL -> MySQL (réplication table)
# IMPORTANT : Utilise ${ENV:...} pour résoudre les secrets depuis .env

sources:
  mysql_source:
    type: mysql
    connection:
      host: ${ENV:MYSQL_HOST}
      port: ${ENV:MYSQL_PORT}
      user: ${ENV:MYSQL_USER}
      password: ${ENV:MYSQL_PASSWORD}
      database: ${ENV:MYSQL_DATABASE}
    extract:
      table: employees
      batch_size: 100
