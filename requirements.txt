# --- Core ---
pyyaml>=6.0
pydantic>=2.0

# --- CLI ---
click>=8.1

# --- Data engines ---
pandas>=2.0
duckdb>=0.10

# --- Logging / utils ---
python-dotenv>=1.0

# --- Tests ---
pytest>=8.0


# --- Code quality ---
black>=24.0
pre-commit>=3.7

pytest-pythonpath>=0.7


# Connecteur MySQL / MariaDB
mysql-connector-python>=8.3.0

# Connecteur PostgreSQL
psycopg2-binary>=2.9.0

# Connecteur MongoDB
pymongo>=4.6.0,<5.0

# Connecteur Parquet / Data Lake
pyarrow>=14.0.0

# Tests HTTP (Web API connector)
requests-mock>=1.11.0

# --- API FastAPI ---
fastapi>=0.111.0
uvicorn[standard]>=0.29.0
# Scheduler des triggers `schedule` (cron)
APScheduler>=3.10.0
# Terminal interactif (ConPTY) — Windows uniquement
pywinpty>=2.0.0; sys_platform == "win32"

#  --- SSH ---
paramiko
