destinations:
  dest_dwh:
    type: mariadb
    connection:
      host: ${ENV:DB_HOST}
      user: etl
      password: ${SECRET:db.password}
      database: dwh
      port: 3306
    load:
      table: orders_dwh
      mode: append
      batch_size: 1000
